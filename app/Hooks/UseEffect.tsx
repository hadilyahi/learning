import { useEffect } from "react";
const UseEffect = () => {
  return (
  <>
        {/* useEffect(callback(what to run), dependencies(when to run)) */}
      
    
    </>
   )
};
export default UseEffect;
